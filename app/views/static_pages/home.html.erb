<div class="top-hero-wrapper text-center">
  <div class="container top-hero-container">
    <div class="row">
      <div class="col-md-12">
        <h1>HAKAMILEへようこそ</h1>

        <h4>「HAKAMILE」はオンライン墓地のウェブサービスです。</h2>

        <% if logged_in? %>
          <%= link_to "MyPageへ", user_path(current_user), class: "btn btn-lg btn-primary" %>
        <% else %>
          <%= link_to "HAKAMILE会員登録", signup_path, class: "btn btn-lg btn-primary" %>
        <% end %>
      </div>

    </div>

  </div>
</div>
