<% if @comment.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      送信内容に<%= @comment.errors.count %>個のエラーがあります。
    </div>
    <ul>
    <% @comment.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
